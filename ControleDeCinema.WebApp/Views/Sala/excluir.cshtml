@model ExcluirSalaViewModel;
@{
    Layout = "_Layout";
    ViewBag.Title = "Exclusão de Sala";
}

<div class="row mt-3">
    <div class="col-lg-6">
        <form class="d-grid gap-4" asp-action="Excluir">
            <input type="hidden" asp-for="Id" />
            <div class="mt-2">
                <label class="control-label fw-semibold">Número:</label>
                <input class="form-control" asp-for="Nome" disabled />
            </div>

            <div class="mt-2">
                <label class="control-label fw-semibold">Capacidade:</label>
                <input class="form-control" asp-for="Capacidade" disabled />
            </div>
            
            <p class="text-bg-danger p-3 rounded fs-5 mt-2">
                Deseja realmente confirmar a exclusão? Não será possível recuperar os dados excluídos posteriormente.
            </p>

            <div class="mt-2 d-grid gap-2 d-md-block">
                <button class="btn btn-danger px-5">
                    <i class="bi bi-trash me-1"></i>
                    Excluir
                </button>

                <a class="btn btn-secondary px-5" href="/sala/listar">
                    <i class="bi bi-arrow-return-left me-1"></i>
                    Voltar
                </a>
            </div>
        </form>
    </div>
</div>
